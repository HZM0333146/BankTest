server:
  port: 9100 # 伺服器的port號

spring:
  jpa:
    generate-ddl: false
    show-sql: true
    hibernate:
      ddl-auto: none
  h2:
    console:
      path: /h2-console
      enabled: true
      settings: 
        web-allow-others: true
  datasource:
    platform: h2
    url: jdbc:h2:mem:testdb
    username: sa
    password:  